diff --git a/equibot/policies/configs/sdp_real_aloha.yaml b/equibot/policies/configs/sdp_real_aloha.yaml
index f3c8eaa..d824aeb 100644
--- a/equibot/policies/configs/sdp_real_aloha.yaml
+++ b/equibot/policies/configs/sdp_real_aloha.yaml
@@ -25,11 +25,9 @@ model:
   ac_mode: 'traj'
   use_torch_compile: true
   
-  # Horizons — pred_horizon=1 since only the first step is used at runtime.
-  # Requires down_dims to be a single element (no temporal halving stages) — see below.
   obs_horizon: 1
-  pred_horizon: 1
-  ac_horizon: 1
+  pred_horizon: 4
+  ac_horizon: 4
 
   # Policy parameters (following reference sdp_ddpm_5layer.yaml)
   canonicalize: false               # Canonicalize point cloud and actions
@@ -47,39 +45,36 @@ model:
   # 5 layers → 3 layers removes the two most expensive (widest channel) stages
   # Requires retraining from scratch.
   encoder:
-    encoder_output_dim: 64         # Output channel dimension (alias: c_dim)
-    c_dim: 64                      # Must match language_encoder_cfg.hidden_size
-    lmax: 1                        # Max degree of spherical harmonics (1 = up to l=1)
-    mmax: 1                        # Max order (same as lmax)
-    max_neighbors: [16, 16, 128]   # KNN neighbors at each scale (3 layers)
-    max_radius: [3, 3, 3]          # Graph radius at each scale
-    pool_ratio: [0.5, 0.5]         # FPS pooling ratio (n_layers-1 = 2 ratios)
-    sphere_channels: [16, 32, 64]  # Channels at each scale (3 layers)
-    attn_hidden_channels: [16, 32, 64]
-    attn_alpha_channels: [4, 8, 16]
-    attn_value_channels: [2, 4, 8]
-    ffn_hidden_channels: [32, 64, 128]
-    edge_channels: [8, 16, 32]
-    num_distance_basis: [64, 64, 64]  # Reduced from 128
+    encoder_output_dim: 128        # Output channel dimension (alias: c_dim)
+    c_dim: 128                     # Same as encoder_output_dim (for compatibility)
+    lmax: 2                        # Max degree of spherical harmonics (2 = up to l=2)
+    mmax: 2                        # Max order (usually same as lmax)
+    max_neighbors: [16, 16, 16, 16, 128]   # KNN neighbors at each scale (5 layers, reference)
+    max_radius: [3, 3, 3, 3, 3]   # Graph radius at each scale (reference: all 3)
+    pool_ratio: [0.5, 0.5, 0.5, 0.5]    # FPS pooling ratio (4 ratios, last is adaptive)
+    sphere_channels: [16, 32, 64, 128, 256]    # Channels at each scale (5 layers, reference)
+    attn_hidden_channels: [16, 32, 64, 128, 256]  # (5 layers, reference)
+    attn_alpha_channels: [4, 8, 16, 32, 64]  # (5 layers, reference)
+    attn_value_channels: [2, 4, 8, 16, 32]  # (5 layers, reference)
+    ffn_hidden_channels: [32, 64, 128, 256, 512]  # (5 layers, reference)
+    edge_channels: [8, 16, 32, 64, 128]  # (5 layers, reference)
+    num_distance_basis: [128, 128, 128, 128, 128]  # (5 layers, all 128, reference)
     num_heads: 4
-    v_grid_resolution:
+    v_grid_resolution: 
     d_grid_resolution:
-    grid_resolution: 8             # Reduced from 12 (cuts S2 grid FFT cost)
-    pool_method: "fpsknn"
+    grid_resolution: 12            # Alias for v_grid_resolution (for compatibility, used as default)
+    pool_method: "fpsknn"          # Pooling method: "fps" or "fpsknn"
     deterministic: false
     alpha_drop: 0.1
     drop_path_rate: 0.0
     proj_drop: 0.1
-
+  
   # Diffusion config (SDTU)
-  # down_dims must be a single element when pred_horizon=1:
-  # with N elements, N-1 stages halve the time axis, requiring pred_horizon >= 2^(N-1).
-  # Single element → 0 halving stages → any pred_horizon including 1.
   diffusion:
-    diffusion_step_embed_dim: 64   # Reduced to match smaller c_dim
-    down_dims: [64]                # Single element: no temporal downsampling
-    kernel_size: 5
-    FiLM_type: 'SFiLM'
+    diffusion_step_embed_dim: 128
+    down_dims: [64, 128, 256]      # U-net channel dimensions
+    kernel_size: 5                 # Kernel size 
+    FiLM_type: 'SFiLM' 
 
   # Language encoder (for skill/task text conditioning)
   language_encoder_cfg:
diff --git a/equibot/policies/utils/sdp_diffusion/irreps_conv1d_components.py b/equibot/policies/utils/sdp_diffusion/irreps_conv1d_components.py
index 97d3256..b1c9501 100644
--- a/equibot/policies/utils/sdp_diffusion/irreps_conv1d_components.py
+++ b/equibot/policies/utils/sdp_diffusion/irreps_conv1d_components.py
@@ -58,16 +58,18 @@ class IrrepConv1dBlock(nn.Module):
     def forward(self, x):
         # x in shape (B, (C, irrep), n_pts)
         assert x.dim() == 3
+        bsz = x.shape[0]
+        npts = x.shape[-1]
         # h = nn.functional.conv1d(x, self.weight, self.bias, padding=self.padding)
         l_start, l_end = 0, 0
         h = []
-        x = einops.rearrange(x, 'b (c i) n -> b c i n', i=self.d_irrep)
+        x = x.reshape(bsz, -1, self.d_irrep, npts)
         for l in range(self.max_lmax + 1):
             l_order = 2 * l + 1
             l_end += l_order
-            x_l = einops.rearrange(x[:, :, l_start:l_end, :], 'b c i n -> (b i) c n')
+            x_l = x[:, :, l_start:l_end, :].permute(0, 2, 1, 3).reshape(bsz * l_order, -1, npts)
             h_l = nn.functional.conv1d(x_l, self.weight[l, :, :, :], padding=self.padding)
-            h_l = einops.rearrange(h_l, '(b i) c n -> (b n) i c', i=l_order)
+            h_l = h_l.reshape(bsz, l_order, self.out_channels, npts).permute(0, 3, 1, 2).reshape(bsz * npts, l_order, self.out_channels)
             if l == 0:
                 h_l = h_l + self.bias
             h.append(h_l)
@@ -77,7 +79,7 @@ class IrrepConv1dBlock(nn.Module):
         if self.activation:
             gating_scalars = self.gating_linear(h.narrow(1, 0, 1))  # This is different from Equiformer
             h = self.s2_act(gating_scalars, h, self.SO3_grid)
-        h = einops.rearrange(h, '(b n) i c -> b (c i) n', n=x.shape[-1])
+        h = h.reshape(bsz, npts, self.d_irrep, self.out_channels).permute(0, 3, 2, 1).reshape(bsz, self.out_channels * self.d_irrep, npts)
         if self.scale != 1:
             h = nn.functional.interpolate(h, scale_factor=self.scale)
         return h
diff --git a/equibot/policies/vision/equiformer_v2/connectivity.py b/equibot/policies/vision/equiformer_v2/connectivity.py
index d80ceb2..4e06d15 100644
--- a/equibot/policies/vision/equiformer_v2/connectivity.py
+++ b/equibot/policies/vision/equiformer_v2/connectivity.py
@@ -6,6 +6,24 @@ from torch_scatter import scatter_add
 from torch_cluster import knn
 
 
+@torch._dynamo.disable
+def _fps_indices(node_coord_src: torch.Tensor, batch_src: torch.Tensor, ratio: float, random_start: bool) -> torch.Tensor:
+    """Run torch_cluster.fps in eager mode to avoid torch.compile FakeTensor failures."""
+    return torch.unique(fps(src=node_coord_src, batch=batch_src, ratio=ratio, random_start=random_start))
+
+
+@torch._dynamo.disable
+def _knn_edges(
+    node_coord_src: torch.Tensor,
+    node_coord_dst: torch.Tensor,
+    max_num_neighbors: int,
+    batch_src: torch.Tensor,
+    batch_dst: torch.Tensor,
+) -> Tuple[torch.Tensor, torch.Tensor]:
+    """Run torch_cluster.knn in eager mode to avoid torch.compile FakeTensor failures."""
+    return knn(node_coord_src, node_coord_dst, max_num_neighbors, batch_x=batch_src, batch_y=batch_dst)
+
+
 class RadiusGraph(torch.nn.Module):
     def __init__(self, r: float, max_num_neighbors: int):
         super().__init__()
@@ -63,8 +81,7 @@ class FpsPool(torch.nn.Module):
         torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor, list]:
         assert node_coord_src.ndim == 2 and node_coord_src.shape[-1] == 3
         if self.ratio != 1.0:
-            node_dst_idx = fps(src=node_coord_src, batch=batch_src, ratio=self.ratio, random_start=self.random_start)
-            node_dst_idx = torch.unique(node_dst_idx)
+            node_dst_idx = _fps_indices(node_coord_src, batch_src, self.ratio, self.random_start)
         else:
             node_dst_idx = torch.arange(0, len(node_coord_src), device=node_coord_src.device)
         # torch_cluster.fps returns a list of indices for each batch, but sometimes the indices are not unique
@@ -135,8 +152,7 @@ class FpsKnnPool(torch.nn.Module):
         torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor, torch.Tensor, list]:
         assert node_coord_src.ndim == 2 and node_coord_src.shape[-1] == 3
         if self.ratio != 1.0:
-            node_dst_idx = fps(src=node_coord_src, batch=batch_src, ratio=self.ratio, random_start=self.random_start)
-            node_dst_idx = torch.unique(node_dst_idx)
+            node_dst_idx = _fps_indices(node_coord_src, batch_src, self.ratio, self.random_start)
         else:
             node_dst_idx = torch.arange(0, len(node_coord_src), device=node_coord_src.device)
         # torch_cluster.fps returns a list of indices for each batch, but sometimes the indices are not unique
@@ -145,8 +161,13 @@ class FpsKnnPool(torch.nn.Module):
         batch_dst = batch_src.index_select(index=node_dst_idx, dim=0)
         N_nodes = len(node_dst_idx)
 
-        edge_dst, edge_src = knn(node_coord_src, node_coord_dst, self.max_num_neighbors,
-                                 batch_x=batch_src, batch_y=batch_dst)
+        edge_dst, edge_src = _knn_edges(
+            node_coord_src=node_coord_src,
+            node_coord_dst=node_coord_dst,
+            max_num_neighbors=self.max_num_neighbors,
+            batch_src=batch_src,
+            batch_dst=batch_dst,
+        )
 
         non_self_idx = (node_dst_idx[edge_dst] != edge_src).nonzero().squeeze(-1)
         edge_src = edge_src[non_self_idx]
diff --git a/equibot/policies/vision/sdp_encoder.py b/equibot/policies/vision/sdp_encoder.py
index ff590f7..9bb35a0 100644
--- a/equibot/policies/vision/sdp_encoder.py
+++ b/equibot/policies/vision/sdp_encoder.py
@@ -36,6 +36,12 @@ from equibot.policies.vision.equiformer_v2.connectivity import (
 )
 
 
+@torch._dynamo.disable
+def _distance_expansion_eager(distance_expansion: nn.Module, edge_length: torch.Tensor) -> torch.Tensor:
+    """Execute distance expansion in eager mode to avoid TorchDynamo ScriptFunction guard issues."""
+    return distance_expansion(edge_length)
+
+
 class SDPEncoder(nn.Module):
     """
     SDP Encoder for object-centric trajectory prediction.
@@ -243,6 +249,7 @@ class SDPEncoder(nn.Module):
         
         print(f"SDPEncoder initialized with {self.num_params} parameters, lmax={lmax}")
     
+    @torch._dynamo.disable
     def forward(self, pcl, target_norm=1.0, language_emb=None):
         """
         Forward pass for object-centric point cloud encoding.
@@ -353,7 +360,7 @@ class SDPEncoder(nn.Module):
                     offset_res = offset_res + int((self.lmax_list[i] + 1) ** 2)
             
             # Distance expansion
-            edge_attr = block['distance_expansion'](edge_length)
+            edge_attr = _distance_expansion_eager(block['distance_expansion'], edge_length)
             
             # Create destination embedding
             node_dst = SO3_Embedding(
